#!/sbin/openrc-run
# PodSteroid runtime tweaks (zram, host share mount, accel hints).
supervisor=supervise-daemon

command="/usr/local/bin/podsteroid-tweaks.sh"
command_args=""

depend() {
    need podsteroid-bootstrap
    after podsteroid-network
}
